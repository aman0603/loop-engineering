[project]
name = "loop-engineering"
version = "0.1.0"
description = "A modular loop engineering framework for autonomous software engineering workflows."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
  "coverage>=7.0",
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
